setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
    - plan: heroku-redis
      as: REDIS
build:
  docker:
    web: StagingDockerfile
  config:
    DJANGO_SETTINGS_MODULE: northpole.settings.docker_development
run:
  web: /northpole/staging-start.sh
