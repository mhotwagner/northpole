setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
    - plan: heroku-redis
      as: REDIS
build:
  docker:
    web: StagingDockerfile
  config:
    DJANGO_SETTINGS_MODULE: northpole.settings.staging
run:
  web: /northpole/staging-start.sh
